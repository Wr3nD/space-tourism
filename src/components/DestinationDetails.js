import React from "react";

import photo from "../assets/destination/image-mars.png";
const DestinationDetails = ({ destination }) => {
    const { name, images, description, distance, travel } =
        destination;
    return (
        <>




        </ >
    );
};

export default DestinationDetails;
